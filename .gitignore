# Bytecode (in the root directory only - not in subdirectories)
/bin/
/build/
/out/

# IntelliJ Idea generated files
.idea/
*.iml

# Eclipse generated files
.classpath
.project

# Gradle generated files
.gradle

# Connection configuration file (as it may contain sensitive information).
# We intentionally share logging.properties as we want to control how do we display messages in the GUI.
connection.properties

# Large generated files
*.csv
*.ddl
*.graphml
*.tgf

#Ignore META-INF
META-INF
